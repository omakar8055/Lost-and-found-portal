<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Statistics</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .stats-container {
            background-color: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 350px;
        }

        .stats-title {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }

        .stats-item {
            margin: 15px 0;
        }

        .stats-label {
            font-size: 18px;
            color: #666;
        }

        .stats-number {
            font-size: 32px;
            font-weight: bold;
            color: #333;
        }

        .stats-percentage {
            font-size: 16px;
            color: #888;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="stats-container">
        <h1 class="stats-title">Statistics</h1>

        <!-- Total Items -->
        <div class="stats-item">
            <div class="stats-label">Total Items</div>
            <div class="stats-number"><%= totalItems %></div>
        </div>
        
        <!-- Lost Items -->
        <div class="stats-item">
            <div class="stats-label">Lost Items</div>
            <div class="stats-number"><%= lostItems %></div>
            <div class="stats-percentage">(<%= lostPercentage.toFixed(2) %>%)</div>
        </div>

        <!-- Found Items -->
        <div class="stats-item">
            <div class="stats-label">Found Items</div>
            <div class="stats-number"><%= foundItems %></div>
            <div class="stats-percentage">(<%= foundPercentage.toFixed(2) %>%)</div>
        </div>

        <!-- Claimed Items -->
        <div class="stats-item">
            <div class="stats-label">Claimed Items</div>
            <div class="stats-number"><%= claimedItems %></div>
            <div class="stats-percentage">(<%= claimedPercentage.toFixed(2) %>%)</div>
        </div>
    </div>
</body>
</html>
